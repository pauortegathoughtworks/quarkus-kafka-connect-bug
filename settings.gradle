pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "${quarkusGroupId}" version "${quarkusPluginVersion}"
    }
}
rootProject.name='code-with-quarkus'
